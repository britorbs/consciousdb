[pytest]
pythonpath = consciousdb-sidecar
filterwarnings =
	ignore:.*atol will change to 0.*:DeprecationWarning:scipy.*
	ignore:.*distutils Version classes are deprecated.*:DeprecationWarning
	# Treat unexpected deprecations as errors (tighten later):
	default::DeprecationWarning
markers =
	real_solver: run test against the real solve_query pipeline (disables stub)
